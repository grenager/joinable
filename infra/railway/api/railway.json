{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/railway/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "uv run alembic -c db/migrations/alembic.ini upgrade head && uv run uvicorn joinable_api.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
