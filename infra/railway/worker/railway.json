{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/railway/worker/Dockerfile"
  },
  "deploy": {
    "startCommand": "uv run celery -A joinable_worker.celery_app:celery_app worker --beat --loglevel=info"
  }
}
